{
  "name": "travel-ai-automation-platform",
  "version": "0.1.3",
  "description": "A TypeScript and PostgreSQL reference implementation for converting Kakao travel inquiries into CRM customer records and booking leads.",
  "private": true,
  "license": "Apache-2.0",
  "homepage": "https://cstion-ai.github.io/cstion/",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cstion-ai/cstion.git"
  },
  "bugs": {
    "url": "https://github.com/cstion-ai/cstion/issues"
  },
  "keywords": [
    "typescript",
    "travel",
    "kakao",
    "crm",
    "booking",
    "webhook",
    "idempotency",
    "postgresql"
  ],
  "engines": {
    "node": ">=22.12.0"
  },
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit && tsc --project tsconfig.test.json",
    "build": "tsc && node scripts/copy-build-assets.mjs && npm run build:web",
    "build:web": "esbuild src/web/reservation-sandbox-entry.ts --bundle --minify --format=esm --platform=browser --target=es2022 --outfile=dist/web/reservation-sandbox.js",
    "check:all": "npm run test:coverage && npm run evaluate:parser && npm run evaluate:challenge && npm run typecheck && npm run build && npm run verify:evaluation-build-parity && cmp src/repositories/migrations/001_harden_existing_schema.sql dist/src/repositories/migrations/001_harden_existing_schema.sql && cmp docs/assets/reservation-sandbox.js dist/web/reservation-sandbox.js && npm audit --audit-level=high",
    "db:migrate": "tsx src/repositories/migrate.ts",
    "db:migrate:prod": "node dist/src/repositories/migrate.js",
    "demo": "tsx src/pipelines/kakao-to-crm.ts",
    "dev": "tsx src/pipelines/kakao-to-crm.ts",
    "evaluate:parser": "tsx src/evaluation/run-reservation-evaluation.ts",
    "evaluate:parser:prod": "node dist/src/evaluation/run-reservation-evaluation.js",
    "evaluate:challenge": "tsx src/evaluation/run-reservation-challenge.ts",
    "evaluate:challenge:prod": "node dist/src/evaluation/run-reservation-challenge.js",
    "verify:evaluation-build-parity": "node scripts/verify-evaluation-build-parity.mjs",
    "start": "node dist/src/server.js",
    "dev:server": "tsx src/server.ts",
    "test": "tsx --test test/*.test.ts",
    "test:coverage": "c8 tsx --test test/*.test.ts",
    "test:postgres": "tsx --test test/postgres-live.integration.ts"
  },
  "c8": {
    "all": true,
    "src": [
      "src"
    ],
    "extension": [
      ".ts"
    ],
    "include": [
      "src/**/*.ts"
    ],
    "exclude": [
      "src/adapters/interfaces.ts"
    ],
    "exclude-after-remap": true,
    "reporter": [
      "text"
    ],
    "check-coverage": true,
    "lines": 90,
    "branches": 80,
    "functions": 85
  },
  "dependencies": {
    "pg": "^8.23.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "@types/pg": "^8.21.0",
    "c8": "^12.0.0",
    "esbuild": "^0.28.2",
    "tsx": "^4.23.11",
    "typescript": "^7.0.2"
  }
}
